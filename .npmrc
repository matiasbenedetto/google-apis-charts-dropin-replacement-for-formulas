# Enable legacy peer deps to avoid issues with canvas installation
legacy-peer-deps=true

# Increase network timeout for larger packages
network-timeout=100000

# Force exact versions for better stability
save-exact=true

# Avoid optional dependencies that might cause issues in serverless environments
omit=optional
